---
// 新しい構造にリダイレクトするエントリーポイント
import KnowledgeGraph from './graph/KnowledgeGraph.astro';
export interface Props {
  issues: any[];
  currentIssueNumber?: number;
  showFullGraph?: boolean;
}

const props = Astro.props;
---

<KnowledgeGraph {...props} />
